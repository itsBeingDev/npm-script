{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "_comment": "comment browser-sync",
    "browser-in-sync": "browser-sync start --server ./prodapp --files \"./prodapp/css/*.css\"",

    "start": "npm-run-all --parallel dev:*",
    "_comment": "comment for sass-dev",
    "dev:sass-dev": "echo sass-converted",
    "_comment": "comment for serve",
    "dev:serve": "echo server-started",

    "build": "npm-run-all prod:*",
    "prod:first": "echo first",
    "prod:second": "echo second"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.18.13"
  },
  "dependencies": {}
}

